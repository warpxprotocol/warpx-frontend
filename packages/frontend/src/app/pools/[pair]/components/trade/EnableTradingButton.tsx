import React from 'react';

export default function EnableTradingButton() {
  return (
    <button
      className="w-full bg-teal-500 hover:bg-teal-600 text-white text-[11px] font-semibold py-1 transition shadow"
      style={{ borderRadius: 0 }}
    >
      Enable Trading
    </button>
  );
}
